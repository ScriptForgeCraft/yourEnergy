# All VITE_* values are public in the browser. Never put a provider, Telegram,
# Turnstile or production credential here. The P1 browser client always calls
# same-origin /api/* Pages Functions; its provider endpoints belong in
# Cloudflare secrets, documented in docs/functions.md.

# Public OpenStreetMap fallback for the manual property/roof map. It is used
# only after the visitor starts the map workflow and must retain attribution.
# Replace it only with an approved HTTPS tile URL; the build adds its origin to
# CSP img-src. This is presentation configuration, never a provider secret.
VITE_MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png
VITE_MAP_ATTRIBUTION=© OpenStreetMap contributors

# Optional same-origin MP4 for the Projects page hero. Put the file under
# public/ and set its public path, e.g. /media/projects-hero.mp4. Leave empty
# to retain the poster-image fallback.
VITE_PROJECTS_HERO_VIDEO=

# Reserved public same-origin URLs for the browser adapter. They must never
# contain credentials. The calculator currently uses a manual point/coordinate
# flow plus optional server-side address search through /api/geocode.
# PVGIS, geocoding, tariff, Telegram and email provider URLs/keys are server-side Functions
# configuration, not VITE_* values.
VITE_ANALYSIS_ENDPOINT=/api/analysis
VITE_POTENTIAL_ENDPOINT=/api/potential
VITE_GEOCODING_ENDPOINT=/api/geocode
VITE_TARIFFS_ENDPOINT=
